
====================================================================
【Q1】
====================================================================
题干：① 你导入完数据只查了行数就完事了吗？还应该查什么？
         ② Price 现在是什么类型？金额能不能用 FLOAT/DOUBLE？
         ③ 能不能给这两张表加 (invoice, stock_code) 主键？为什么？
   提示：行数、MIN/MAX(日期)、天数跨度、排序规则 —— 这四个都要看。
        SHOW FULL COLUMNS FROM 表名;
        SELECT MIN(invoice_date), MAX(invoice_date),
               DATEDIFF(MAX(invoice_date), MIN(invoice_date)) FROM 表名;
   ---------------------------------------------------------------------------

====================================================================
【Q2】
====================================================================
题干：① 用一条 SQL 同时报出两张表各有多少行。
         ② 合并后（retail_raw）有多少行？完全去重后有多少行？差多少？
         ③ 差额从哪来？帮它定位到具体月份。
   提示：UNION ALL 做 ①；SELECT DISTINCT * 做 ②；
        GROUP BY 全 8 列 HAVING COUNT(*)>1 做 ③。
   ---------------------------------------------------------------------------

====================================================================
【Q3】
====================================================================
题干：① 分别查出两张表的最早/最晚开票时间，以及合并后的范围。
         ② 两张表的时间范围有重叠吗？重叠哪几天？
         ③ 这个重叠对"按月分析"意味着什么？
   ---------------------------------------------------------------------------

====================================================================
【Q4】
====================================================================
题干：用 ONE 条 SQL 同时输出 retail_raw 的：总行数、唯一发票数、
        唯一商品编码数、唯一客户 ID 数、唯一国家数。
   提示：COUNT(DISTINCT …) 五个写在一起即可；注意空值会不会被 COUNT 掉。
   ---------------------------------------------------------------------------

====================================================================
【Q6】
====================================================================
题干：验证「一张发票 = 一个客户 + 一个国家」是否成立。
        即：同一个 invoice 是否出现过 >1 个 customer_id？是否出现过 >1 个 country？
   提示：GROUP BY invoice HAVING COUNT(DISTINCT …) > 1
   追问：除了反例，还有没有"看着像商品、其实不是商品"的行？找出来。
   ---------------------------------------------------------------------------

====================================================================
【Q9】
====================================================================
题干：统计 customer_id 为空的行数，以及它占总行数的百分比（保留 2 位）。
   追问：为什么建表时把客户 ID 存成 INT（NULL）比存成字符串更干净？
   ---------------------------------------------------------------------------

====================================================================
【Q11】
====================================================================
题干：在 retail_raw 上同时输出：
        C 开头总行数 / 其中 quantity<0 / 其中 quantity>0 / 其中 quantity=0 /
        非 C 开头但 quantity<0 / C 单里客户 ID 为空的行数
   追问：为什么清洗条件必须同时写「不是 C 单」和「数量 > 0」两个条件？
   ---------------------------------------------------------------------------

====================================================================
【Q13】
====================================================================
题干：建 retail_clean（9 列，含派生列 amount），把 retail_raw 清洗后灌进去。
        清洗 = 4 条 WHERE + 1 次去重，字段要做 TRIM。
   追问：① 为什么要 TRIM？商品名里的空格是前导还是尾随？（说清"全表有 21.3 万行带空格、
          其中 21.1 万行是尾随空格"；编码里还有 1 个 '47503J ' 带尾空格）
        ② 为什么清洗后国家数从 43 变成 41？
   注意：先建表再 INSERT —— CREATE TABLE ... AS SELECT 不继承索引和字符集。
   ⚠️⚠️ 这段是「DROP + CREATE + INSERT」三句一体的，【必须整段一起跑】！
        2026-09-21 事故：只跑到 CREATE 就停手 → 真表被删、新表为空，且不报错。
        跑完自查：SELECT COUNT(*) FROM retail_clean;  必须是 779,425
   本机最安全练法：把表名改成 retail_clean_test 练一遍 → 数字对了再改回 retail_clean 正式落一次。
   （本题 = 自己重做 SQL_00 第 2 段，答案可对照）
   ---------------------------------------------------------------------------

====================================================================
【Q14】
====================================================================
题干：① 剔掉散客会损失多少营业额？占有效正向流水的百分之几？
        （提示：不用新写 SQL，用 Q15 的两个金额相减即可）
        ② 如果不剔散客，会破坏哪些指标？
        ③ 用 2 句话把这件事写进你的作品集（交代口径局限）。
   ---------------------------------------------------------------------------

====================================================================
【Q18】
====================================================================
题干：按客户汇总流水和订单数，用 RANK 排名，输出前 20。
   追问：① ROW_NUMBER / RANK / DENSE_RANK 三者区别？什么场景必须用哪个？
        ② 榜单里有没有"单数极少但金额巨大"的客户？这说明什么？
   ---------------------------------------------------------------------------

====================================================================
【Q20】
====================================================================
题干：三个 CTE（首单月 / 活跃月 / 关联），输出每个同期群的 M0~M6 留存人数。
   提示：PERIOD_DIFF(REPLACE(act_m,'-',''), REPLACE(cohort_m,'-',''))
   追问：① 为什么 M1 会比 M2 低？这是数据错误吗？
        ② 矩阵右下角那一堆 0 是什么？能填 0 吗？
   ---------------------------------------------------------------------------

====================================================================
【Q21】
====================================================================
题干：按月输出 流水 / 订单数 / 客户数 / 客单价，按月份升序。
   追问：① 你看得出什么季节性？② 两年 11 月哪个指标在恶化？
   ---------------------------------------------------------------------------

====================================================================
【Q25】
====================================================================
题干：算出每个客户的 R（距快照日天数）、F（发票数）、M（金额），
        按 M 降序取前 10。
   追问：① R 的基准日为什么不能用 NOW()？
        ② R = 0 的人是什么情况？
        ③ DATEDIFF 会忽略时分秒 —— 客户 14646 最后一单在 12-08，R 应该是 1 还是 0？
           把口径写下来，Day 8 做 RFM 分箱要靠它。
   ---------------------------------------------------------------------------

====================================================================
【Q26】
====================================================================
题干：写 4 段话，每段都必须带具体数字：
        ① 钱从哪来？② 最大风险是什么？③ 数据本身有什么坑（最加分的一问）
        ④ 下一步先做什么分析（要具体到能写 SQL）
   提示：数字全部来自前面 25 题，不用重算。
   ---------------------------------------------------------------------------
